# epice.dago-cloud.com — Zara Spices Export
# Reverse proxy → conteneur Docker sur 127.0.0.1:3010
# Certbot ajoutera le bloc HTTPS automatiquement

server {
    listen 80;
    listen [::]:80;
    server_name epice.dago-cloud.com;

    # Logs séparés des autres projets
    access_log /var/log/nginx/epice.dago-cloud.com.access.log;
    error_log  /var/log/nginx/epice.dago-cloud.com.error.log;

    location / {
        proxy_pass         http://127.0.0.1:3010;
        proxy_http_version 1.1;

        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        'upgrade';
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
        proxy_connect_timeout 10s;
    }

    # Cache agressif pour les assets statiques Next.js
    location /_next/static/ {
        proxy_pass       http://127.0.0.1:3010;
        add_header       Cache-Control "public, max-age=31536000, immutable";
        proxy_cache_bypass 0;
        proxy_no_cache   0;
    }
}
